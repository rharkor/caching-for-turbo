{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "test-build": {
      "inputs": ["src"],
      "outputs": ["dist"]
    },
    "test-unit": {
      "inputs": ["src", "__tests__", "__mocks__", "jest.config.ts"]
    }
  }
}
